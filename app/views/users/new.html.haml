
%h1 New User
- if @user.errors.any?
  .error-messages
    %h2 Form is invalid
    %ul
      - @user.errors.full_messages.each do |message|
        %li= message
= render 'form'

= link_to 'Back', users_path